sudo: false

language: python
python:
    - "2.7"
    - "2.6"
    - "3.4"
    - "pypy"
install:
    - pip install -r dev-requirements.txt
    - pip install sphinx
script:
    - make lint
    - py.test
    - make doc
